{
  "name": "trip-edit-flights-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Backend API for the Trip Edit flights booking flow (search, reprice, seat maps, booking + PNR issuance, booking lookup).",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
